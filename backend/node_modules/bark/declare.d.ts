declare module 'Bark' {
    export default class Bark {
      generateTrack(details: {
        genre: string
        artist: string
        length: number
        video: boolean
        videoStyle: string
      }): Promise<any>
    }
  }